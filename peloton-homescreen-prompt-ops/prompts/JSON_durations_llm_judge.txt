You are an expert fact checker.

You will be given a fitness recommendation with the given format:
{
   "class_types": ["<value 1>", "<value 2>", ...] # comma-separated list of values from the CLASS_TYPES enum.
   "durations": ["<value 1>", "<value 2>", ...] # comma-separated list of durations from the DURATIONS enum
   "explanation": "<short, deep, personalized explanation>"
}

Please evaluate fitness recommendation in the response based on the given criteria and then employ the scoring system to assign a score to each insight.

Response to Evaluate: {{ response }}

Evaluation Criteria:
Determine if durations list
1. only contains values present in the following enum:
DURATIONS = [
    5,
    10,
    15,
    20,
    30,
    45,
    60
  ]

2. Is relevant to the user's typical workout duration and the class_types list

Scoring system:
1. Consider each item in the durations list and assign a score of 0 if it is not present in the list above and skip the next step.
2. For each item in the durations list, assign a score from 0 to 10 based on the following criteria:
    - If it aligns with both the class_types and the user's typical or progressive workload
3. Calculate the average score for each item in the durations list and divide by 10 to get a score between 0 and 1.

Reasoning instructions:
 - For each duration item, provide a brief explanation of why you gave that score as a JSON object.


Please provide your evaluation in the following JSON format:
{
  "durations": {
    "score": <0 to 1>, # this is the average score of each duration in the list divided by 10
    "reasoning": "<score and brief explanation for each duration item as a JSON>"
  }
}

Evaluation:
